name: GitHub Actions Lint Build Test

on:
  push:
    branches: ["test", "main", "release"]
  pull_request:
    types: [opened, synchronize, reopened, edited]

jobs:
  lint-test-use-backend:
    uses: ENSE7011/Assignment-1/backend/lint-build-test-backend.yml

  lint-test-use-frontend:
    needs: lint-test-use-backend
    uses: ENSE7011/Assignment-1/backend/lint-build-test-frontend.yml
